// console.log(process.env.PORT);

const { SHELL, MEMORY_PRESSURE_WRITE } = process.env;
// console.table({ SHELL, MEMORY_PRESSURE_WRITE });

export const characters = ["flash", "superman", "spiderman", "thor", "batman"];
const [, , batman] = characters;
// console.log(superman);
